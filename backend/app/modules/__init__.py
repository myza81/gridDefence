"""Business modules live here as sibling packages, one per bounded context
(iam, substation_registry, equipment_registry, psse_integration,
network_model, ufls, uvls, emls, critical_infrastructure,
cross_scheme_compliance, dashboard), per CLAUDE.md §12 and
docs/architecture/implementation-plan.md §1-2.

Phase 0 intentionally creates no module packages yet — they are added one at
a time, per their own implementation phase.
"""

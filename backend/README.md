# GridDefence Backend

Backend service for the GridDefence platform.

This package contains the FastAPI application, database models, Alembic migrations, and backend business logic.

This README exists to satisfy Python package metadata (`pyproject.toml`) during editable installs.